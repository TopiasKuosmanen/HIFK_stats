/*
Deployment script for HIFK

This code was generated by a tool.
Changes to this file may cause incorrect behavior and will be lost if
the code is regenerated.
*/

GO
SET ANSI_NULLS, ANSI_PADDING, ANSI_WARNINGS, ARITHABORT, CONCAT_NULL_YIELDS_NULL, QUOTED_IDENTIFIER ON;

SET NUMERIC_ROUNDABORT OFF;


GO
:setvar DatabaseName "HIFK"
:setvar DefaultFilePrefix "HIFK"
:setvar DefaultDataPath "C:\Users\Topias Kuosmanen\AppData\Local\Microsoft\Microsoft SQL Server Local DB\Instances\MSSQLLocalDB"
:setvar DefaultLogPath "C:\Users\Topias Kuosmanen\AppData\Local\Microsoft\Microsoft SQL Server Local DB\Instances\MSSQLLocalDB"

GO
:on error exit
GO
/*
Detect SQLCMD mode and disable script execution if SQLCMD mode is not supported.
To re-enable the script after enabling SQLCMD mode, execute the following:
SET NOEXEC OFF; 
*/
:setvar __IsSqlCmdEnabled "True"
GO
IF N'$(__IsSqlCmdEnabled)' NOT LIKE N'True'
    BEGIN
        PRINT N'SQLCMD mode must be enabled to successfully execute this script.';
        SET NOEXEC ON;
    END


GO
USE [$(DatabaseName)];


GO
/*
The column [dbo].[Playersposition].[Position] is being dropped, data loss could occur.

The column [dbo].[Playersposition].[PlayerId] on table [dbo].[Playersposition] must be added, but the column has no default value and does not allow NULL values. If the table contains data, the ALTER script will not work. To avoid this issue you must either: add a default value to the column, mark it as allowing NULL values, or enable the generation of smart-defaults as a deployment option.
*/

IF EXISTS (select top 1 1 from [dbo].[Playersposition])
    RAISERROR (N'Rows were detected. The schema update is terminating because data loss might occur.', 16, 127) WITH NOWAIT

GO
/*
The column [dbo].[Position].[PlayerId] is being dropped, data loss could occur.

The column [dbo].[Position].[Position] on table [dbo].[Position] must be added, but the column has no default value and does not allow NULL values. If the table contains data, the ALTER script will not work. To avoid this issue you must either: add a default value to the column, mark it as allowing NULL values, or enable the generation of smart-defaults as a deployment option.
*/

IF EXISTS (select top 1 1 from [dbo].[Position])
    RAISERROR (N'Rows were detected. The schema update is terminating because data loss might occur.', 16, 127) WITH NOWAIT

GO
PRINT N'Starting rebuilding table [dbo].[Playersposition]...';


GO
BEGIN TRANSACTION;

SET TRANSACTION ISOLATION LEVEL SERIALIZABLE;

SET XACT_ABORT ON;

CREATE TABLE [dbo].[tmp_ms_xx_Playersposition] (
    [PlayerId]   INT NOT NULL,
    [PositionId] INT NOT NULL,
    PRIMARY KEY CLUSTERED ([PlayerId] ASC)
);

IF EXISTS (SELECT TOP 1 1 
           FROM   [dbo].[Playersposition])
    BEGIN
        INSERT INTO [dbo].[tmp_ms_xx_Playersposition] ([PositionId])
        SELECT [PositionId]
        FROM   [dbo].[Playersposition];
    END

DROP TABLE [dbo].[Playersposition];

EXECUTE sp_rename N'[dbo].[tmp_ms_xx_Playersposition]', N'Playersposition';

COMMIT TRANSACTION;

SET TRANSACTION ISOLATION LEVEL READ COMMITTED;


GO
PRINT N'Starting rebuilding table [dbo].[Position]...';


GO
BEGIN TRANSACTION;

SET TRANSACTION ISOLATION LEVEL SERIALIZABLE;

SET XACT_ABORT ON;

CREATE TABLE [dbo].[tmp_ms_xx_Position] (
    [PositionId] INT        NOT NULL,
    [Position]   NCHAR (50) NOT NULL,
    PRIMARY KEY CLUSTERED ([PositionId] ASC)
);

IF EXISTS (SELECT TOP 1 1 
           FROM   [dbo].[Position])
    BEGIN
        INSERT INTO [dbo].[tmp_ms_xx_Position] ([PositionId])
        SELECT   [PositionId]
        FROM     [dbo].[Position]
        ORDER BY [PositionId] ASC;
    END

DROP TABLE [dbo].[Position];

EXECUTE sp_rename N'[dbo].[tmp_ms_xx_Position]', N'Position';

COMMIT TRANSACTION;

SET TRANSACTION ISOLATION LEVEL READ COMMITTED;


GO
PRINT N'Update complete.';


GO
